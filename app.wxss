/**app.wxss**/
.container {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
  background-color: #ffffff;
}

/* 统一盒模型，避免后续 padding 撑破容器 */
view, text, scroll-view, input, button {
  box-sizing: border-box;
}